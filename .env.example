# Copy this file to .env and fill in the value.
# In CI/CD, supply VRSRC_API_KEY as a repository secret.
# This key is injected into the frontend bundle at build time via Vite's define —
# it never appears in source code or git history.
VRSRC_API_KEY=
